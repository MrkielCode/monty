#include "monty.h"
/**
 * c_stack - print the top of the loop
 * @head: pointer to the head
 * @count: line number
 * Return: no return
 */

void c_stack(stack_t **head, unsigned int count)
{
	(void)head;
	(void)count;
	args.status = 0;
}
