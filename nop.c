#include "monty.h"
/**
  *c_nop - to do nothing
  *@head: pointer to the head of the stack
  *@count: line umber
  *Return: void;
 */
void c_nop(stack_t **head, unsigned int count)
{
	(void) count;
	(void) head;
}
